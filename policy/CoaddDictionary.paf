#<?cfg paf dictionary ?>

definitions: {
    badMaskPlanes: {
        type: string
        description: "Mask planes to pay attention to when rejecting masked pixels.
            It should always include EDGE."
        minOccurs: 0
        default: "EDGE"
    }
}
